# PM Orchestrator Architecture v3.0.0 Design Document

## Overview

This document defines the architecture for PM Orchestrator v3.0.0, which extends the Skills-First with Fallback Architecture v2.1.0 with:

1. Session and TaskRun management
2. Task continuation detection
3. Task tracker integration (ClickUp/Asana via MCP)
4. Task monitoring and alerting
5. E2E test workflow (Playwright)
6. Code review workflows (2 patterns)
7. Project configuration management

## Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              User Input                                      │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     user-prompt-submit.sh (Hook)                             │
│  - Pattern detection                                                         │
│  - Watcher startup check                                                     │
│  - PM trigger                                                                │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PM Orchestrator v3.0.0                               │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ Session Manager                                                      │    │
│  │  - sessionId management                                              │    │
│  │  - taskRunId generation                                              │    │
│  │  - Continuation detection (same_task / new_task / unknown)           │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ TaskType Determination                                               │    │
│  │  - READ_INFO / LIGHT_EDIT / IMPLEMENTATION / REVIEW_RESPONSE         │    │
│  │  - CONFIG_CI_CHANGE / DANGEROUS_OP                                   │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ Workflow Orchestration                                               │    │
│  │  - Pipeline selection based on TaskType                              │    │
│  │  - Subagent chain execution                                          │    │
│  │  - Result aggregation                                                │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
┌───────────────────────┐ ┌───────────────────┐ ┌───────────────────────┐
│   Planning Phase      │ │  Execution Phase  │ │   Quality Phase       │
│  🔵 BLUE              │ │  🟠 ORANGE        │ │  🟢 GREEN             │
│  - task-decomposer    │ │  - implementer    │ │  - qa                 │
│  - work-planner       │ │                   │ │  - e2e-test-runner    │
│  - requirement-analyzer│ │                   │ │  - code-reviewer      │
│  - technical-designer │ │                   │ │                       │
└───────────────────────┘ └───────────────────┘ └───────────────────────┘
                    │               │               │
                    └───────────────┼───────────────┘
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Integration Phase                                    │
│  🟡 YELLOW                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ task-tracker-sync                                                    │    │
│  │  - Create/update tasks in ClickUp/Asana                              │    │
│  │  - Add comments with progress                                        │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ reporter                                                             │    │
│  │  - Final report generation                                           │    │
│  │  - Task tracker final comment                                        │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Background Services                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ task-run-watcher.sh                                                  │    │
│  │  - Monitor .claude/sessions/*.json                                   │    │
│  │  - Detect stale runs                                                 │    │
│  │  - Trigger task-run-monitor skill                                    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ task-run-monitor Skill                                               │    │
│  │  - Process stale run alerts                                          │    │
│  │  - Add comments to task tracker                                      │    │
│  │  - Optional: Slack notification                                      │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

## New Files and Directories

### Directory Structure

```
.claude/
├── sessions/                           # NEW: Session storage
│   └── <sessionId>.json               # Session files with runs
├── scripts/                            # NEW: Utility scripts
│   ├── task-run-watcher.sh            # Background watcher
│   └── generate-session-id.sh         # Session ID generator
├── skills/
│   ├── pm-orchestrator.md             # UPDATED: v3.0.0
│   ├── task-tracker-sync.md           # NEW: Task tracker integration
│   ├── task-run-monitor.md            # NEW: Stale task monitor
│   ├── e2e-test-runner.md             # NEW: E2E test execution
│   ├── code-review-manager.md         # NEW: Code review workflow
│   ├── project-config-manager.md      # NEW: Project configuration
│   ├── session-manager.md             # NEW: Session/TaskRun management
│   └── ... (existing skills)
├── commands/
│   ├── pm-config.md                   # NEW: /pm-config command
│   └── pm-session.md                  # NEW: /pm-session command
├── hooks/
│   └── user-prompt-submit.sh          # UPDATED: Watcher startup
├── project-config.json                 # NEW: Project configuration
└── CLAUDE.md                          # UPDATED: v3.0.0
```

### New Skills

| Skill | Category | Purpose |
|-------|----------|---------|
| session-manager | orchestration | Session/TaskRun management |
| task-tracker-sync | integration | ClickUp/Asana integration via MCP |
| task-run-monitor | monitoring | Stale task detection and alerting |
| e2e-test-runner | quality | Playwright E2E test execution |
| code-review-manager | review | Code review workflow management |
| project-config-manager | configuration | Project settings management |

## Data Structures

### Session JSON Schema

```json
{
  "sessionId": "string",
  "createdAt": "ISO8601",
  "updatedAt": "ISO8601",
  "runs": [
    {
      "taskRunId": "string (YYYY-MM-DD-NNN)",
      "title": "string",
      "status": "running | done | blocked | partial | abandoned",
      "createdAt": "ISO8601",
      "updatedAt": "ISO8601",
      "taskTrackerTaskId": "string | null",
      "meta": {
        "repoPath": "string",
        "targetDir": "string | null",
        "taskType": "TaskType",
        "continuationMode": "same_task | new_task | unknown",
        "colorTag": "blue | green | yellow | orange | red",
        "role": "string"
      },
      "steps": [
        {
          "skillId": "string",
          "status": "done | skipped | failed | pending",
          "startedAt": "ISO8601",
          "completedAt": "ISO8601 | null",
          "output": "string | null"
        }
      ]
    }
  ]
}
```

### Project Configuration Schema

```json
{
  "version": "3.0.0",
  "e2eTest": {
    "enabled": true,
    "browser": "chrome | firefox | webkit",
    "headless": true,
    "extraBrowsers": []
  },
  "codeReview": {
    "mode": "none | local_pr | review_remote",
    "reviewRemoteName": "review-origin",
    "autoResolve": false
  },
  "taskTracker": {
    "provider": "clickup | asana | none",
    "projectId": "string",
    "defaultList": "string",
    "mcpServerName": "string"
  },
  "monitor": {
    "enabled": true,
    "staleMinutes": 45,
    "notifySlack": false,
    "slackWebhook": "string | null"
  },
  "session": {
    "autoNewTaskOnTaskTypeChange": true,
    "staleSessionMinutes": 60,
    "defaultContinuationMode": "same_task"
  }
}
```

## Continuation Detection Logic

### Decision Tree

```
User Input Received
        │
        ▼
┌───────────────────────────────────────┐
│ Check explicit keywords               │
│ "別件" "新しいタスク" "さっきとは別に" │
└───────────────────────────────────────┘
        │ Yes → new_task
        │ No ↓
┌───────────────────────────────────────┐
│ Check continuation keywords           │
│ "続き" "さっきの" "同じタスクで"      │
└───────────────────────────────────────┘
        │ Yes → same_task
        │ No ↓
┌───────────────────────────────────────┐
│ Check time elapsed                    │
│ > staleSessionMinutes?                │
└───────────────────────────────────────┘
        │ Yes → new_task
        │ No ↓
┌───────────────────────────────────────┐
│ Check TaskType change                 │
│ READ_INFO → IMPLEMENTATION?           │
│ (if autoNewTaskOnTaskTypeChange)      │
└───────────────────────────────────────┘
        │ Yes → new_task
        │ No ↓
┌───────────────────────────────────────┐
│ Check context change                  │
│ Different repo/directory?             │
└───────────────────────────────────────┘
        │ Yes → new_task
        │ No ↓
        ▼
    same_task (default)
```

## Code Review Workflows

### Pattern A: Local PR (GitHub/GitLab)

```
Implementation Complete
        │
        ▼
┌───────────────────────────────────────┐
│ Create feature branch                 │
│ git checkout -b feature/xxx           │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Push to origin                        │
│ git push -u origin feature/xxx        │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Create PR                             │
│ gh pr create                          │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Wait for CodeRabbit/Copilot review    │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Address feedback                      │
│ (Loop until all resolved)             │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Merge PR                              │
└───────────────────────────────────────┘
```

### Pattern B: Review Remote

```
Implementation Complete
        │
        ▼
┌───────────────────────────────────────┐
│ Create feature branch                 │
│ git checkout -b feature/xxx           │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Push to review-origin (NOT origin)    │
│ git push review-origin feature/xxx    │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Create PR on review remote            │
│ (CodeRabbit reviews here)             │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Address feedback locally              │
│ Push fixes to review-origin           │
│ (Loop until all resolved)             │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Close PR on review-origin (no merge)  │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Push to origin (main remote)          │
│ git push origin feature/xxx           │
│ Create PR and merge on origin         │
└───────────────────────────────────────┘
```

## E2E Test Workflow

```
QA Phase Start
        │
        ▼
┌───────────────────────────────────────┐
│ Check e2eTest.enabled in config       │
└───────────────────────────────────────┘
        │ No → Skip E2E
        │ Yes ↓
┌───────────────────────────────────────┐
│ e2e-test-runner Skill                 │
│ - npx playwright test --headless      │
│ - Browser: config.e2eTest.browser     │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Parse results                         │
│ - Pass/Fail count                     │
│ - Failed test details                 │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Return to QA Skill                    │
│ Include E2E results in QA report      │
└───────────────────────────────────────┘
```

## Implementation Phases

### Phase 1: Core Infrastructure
- [ ] Session storage directory and JSON schema
- [ ] session-manager Skill
- [ ] project-config.json and schema
- [ ] project-config-manager Skill

### Phase 2: Session Management
- [ ] sessionId generation
- [ ] taskRunId generation
- [ ] Continuation detection logic
- [ ] pm-orchestrator v3.0.0 update

### Phase 3: Task Tracker Integration
- [ ] task-tracker-sync Skill
- [ ] MCP integration for ClickUp/Asana
- [ ] Create/update task logic
- [ ] Comment management

### Phase 4: Monitoring
- [ ] task-run-watcher.sh script
- [ ] task-run-monitor Skill
- [ ] Hook update for watcher startup
- [ ] Stale detection logic

### Phase 5: E2E Testing
- [ ] e2e-test-runner Skill
- [ ] Playwright integration
- [ ] QA workflow integration

### Phase 6: Code Review
- [ ] code-review-manager Skill
- [ ] Pattern A (local PR) workflow
- [ ] Pattern B (review remote) workflow
- [ ] Feedback loop implementation

### Phase 7: Commands and UI
- [ ] /pm-config command
- [ ] /pm-session command
- [ ] Color tag integration
- [ ] Role display

### Phase 8: Documentation and Testing
- [ ] Update CLAUDE.md to v3.0.0
- [ ] Update WORKFLOWS.md
- [ ] Integration testing
- [ ] Final report

## Version History

| Version | Date | Changes |
|---------|------|---------|
| 3.0.0 | 2025-12-08 | Initial design for session management, task tracking, monitoring |

## References

- Skills-First Architecture v2.1.0
- SKILL_METADATA_FORMAT.md
- WORKFLOWS.md
- Claude Code Skills Documentation: https://docs.anthropic.com/claude/docs/claude-code-skills
