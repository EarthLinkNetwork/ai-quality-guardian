---
skill: code-review-manager
version: 1.0.0
category: review
description: コードレビューワークフローを管理し、通常PR方式とレビュー専用リポジトリ方式の2パターンをサポート
metadata:
  id: code-review-manager
  display_name: Code Review Manager
  risk_level: medium
  color_tag: GREEN
  task_types:
    - IMPLEMENTATION
    - REVIEW_RESPONSE
    - CONFIG_CI_CHANGE
capabilities:
  - branch_management
  - pr_creation
  - review_tracking
  - feedback_resolution
tools:
  - Read
  - Bash
  - Task
priority: medium
activation: on_demand
dependencies:
  - skill: pm-orchestrator
    relationship: called_by
  - skill: implementer
    relationship: receives_input_from
  - skill: code-reviewer
    relationship: cooperates_with
---

# Code Review Manager - コードレビュー管理スキル

## Activation Conditions

pm-orchestrator から以下の条件で起動される:
1. implementer Skill の実装完了後
2. project-config.json で codeReview.mode が `none` 以外
3. TaskType が IMPLEMENTATION / REVIEW_RESPONSE / CONFIG_CI_CHANGE

## Review Modes

| Mode | Description | Use Case |
|------|-------------|----------|
| `none` | レビューなし | 小規模プロジェクト、個人開発 |
| `local_pr` | 通常のPR方式 | GitHub/GitLab でCodeRabbit等が有効 |
| `review_remote` | レビュー専用リポジトリ | メインリポジトリにレビューツールがない場合 |

## Configuration

```json
{
  "codeReview": {
    "mode": "none | local_pr | review_remote",
    "reviewRemoteName": "review-origin",
    "autoResolve": false
  }
}
```

---

## Pattern A: Local PR (local_pr)

### Overview

通常の GitHub/GitLab ワークフロー。
origin にプッシュし、PR を作成してレビューを受ける。

### Flow Diagram

```
┌─────────────────────────────────────────┐
│ 1. implementer 完了                      │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 2. フィーチャーブランチ作成              │
│    git checkout -b feature/xxx          │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 3. origin にプッシュ                     │
│    git push -u origin feature/xxx       │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 4. PR 作成                              │
│    gh pr create                         │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 5. CodeRabbit/Copilot レビュー待ち      │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 6. 指摘対応ループ                        │
│    - 指摘を確認                         │
│    - 修正を実装                         │
│    - push                               │
│    - 全指摘 resolved まで繰り返し        │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 7. PR マージ                            │
└─────────────────────────────────────────┘
```

### Commands

```bash
# ブランチ作成
git checkout -b feature/login-validation

# コミット
git add .
git commit -m "feat: add login validation"

# プッシュ
git push -u origin feature/login-validation

# PR作成
gh pr create --title "feat: add login validation" --body "..."

# レビュー状態確認
gh pr checks
gh pr view --comments

# 指摘対応後
git add .
git commit -m "fix: address review feedback"
git push

# マージ
gh pr merge --squash
```

---

## Pattern B: Review Remote (review_remote)

### Overview

メインリポジトリには CodeRabbit 等がない場合に使用。
レビュー専用リモート（例: review-origin）にプッシュしてレビューを受ける。

### Key Principles

1. **review-origin は Push Only**: pull は絶対にしない
2. **メインリポジトリが真実のソース**: 常に origin が正
3. **review-origin の PR はマージしない**: クローズのみ

### Flow Diagram

```
┌─────────────────────────────────────────┐
│ 1. implementer 完了                      │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 2. フィーチャーブランチ作成              │
│    git checkout -b feature/xxx          │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 3. review-origin にプッシュ              │
│    git push review-origin feature/xxx   │
│    ⚠️ origin には まだ push しない       │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 4. review-origin で PR 作成             │
│    gh pr create -R review-origin-repo   │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 5. CodeRabbit レビュー待ち              │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 6. 指摘対応ループ                        │
│    - ローカルで修正                      │
│    - review-origin に push              │
│    - 全指摘 resolved まで繰り返し        │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 7. review-origin の PR をクローズ        │
│    ⚠️ マージしない！                     │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 8. origin にプッシュ & PR 作成           │
│    git push -u origin feature/xxx       │
│    gh pr create                         │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│ 9. origin で PR マージ                  │
└─────────────────────────────────────────┘
```

### Commands

```bash
# リモート確認
git remote -v

# review-origin がない場合は追加
git remote add review-origin git@github.com:user/review-repo.git

# ブランチ作成
git checkout -b feature/login-validation

# review-origin にプッシュ（origin ではない！）
git push review-origin feature/login-validation

# review-origin で PR 作成
gh pr create -R user/review-repo --title "feat: add login validation"

# 指摘対応後
git add .
git commit -m "fix: address review feedback"
git push review-origin feature/login-validation

# レビュー完了後、review-origin の PR をクローズ
gh pr close -R user/review-repo

# origin にプッシュ（最終版）
git push -u origin feature/login-validation

# origin で PR 作成 & マージ
gh pr create
gh pr merge --squash
```

### Important Warnings

```
⚠️ review-origin からの pull は禁止！

以下のコマンドは絶対に実行しないでください:
❌ git pull review-origin
❌ git fetch review-origin && git merge
❌ git rebase review-origin/main

常にローカル/origin が真実のソースです。
```

---

## Output Format

### Mode: none

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Code Review Manager - スキップ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

コードレビューは無効です。
有効にするには /pm-config edit で codeReview.mode を設定してください。

Status: skipped
```

### Mode: local_pr

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Code Review Manager - PR 作成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【モード】
Local PR (通常のGitHubワークフロー)

【ブランチ】
feature/login-validation

【PR 情報】
URL: https://github.com/user/repo/pull/123
タイトル: feat: add login validation

【次のステップ】
1. CodeRabbit/Copilot のレビューを待つ
2. 指摘があれば対応
3. 全て resolved になったらマージ

Status: completed
```

### Mode: review_remote

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Code Review Manager - Review Remote PR 作成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【モード】
Review Remote (レビュー専用リポジトリ)

【ブランチ】
feature/login-validation

【Review Remote PR】
URL: https://github.com/user/review-repo/pull/45
リモート: review-origin

【重要】
⚠️ このPRはマージしないでください！
⚠️ review-origin から pull しないでください！

【次のステップ】
1. CodeRabbit のレビューを待つ
2. 指摘があれば ローカルで修正 → review-origin に push
3. 全て resolved になったら:
   - review-origin の PR をクローズ（マージしない）
   - origin に push
   - origin で PR 作成 & マージ

Status: completed
```

### Feedback Resolution

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Code Review Manager - 指摘対応状況
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【PR】
https://github.com/user/repo/pull/123

【指摘状況】
- 全体: 5件
- ✅ Resolved: 3件
- ⏳ Pending: 2件

【未対応の指摘】
1. src/components/LoginForm.tsx:42
   「バリデーションエラーのメッセージが不明確」

2. src/api/auth.ts:28
   「エラーハンドリングを追加してください」

【次のアクション】
上記の指摘に対応し、push してください。

Status: in_progress
```

---

## Integration Points

- **入力元**: pm-orchestrator, implementer
- **出力先**: reporter, task-tracker-sync

## Error Handling

### review-origin が設定されていない場合

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Code Review Manager - エラー
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【エラー】
リモート 'review-origin' が設定されていません。

【対処方法】
git remote add review-origin git@github.com:user/review-repo.git

Status: error
```

### gh CLI が利用できない場合

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Code Review Manager - エラー
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【エラー】
GitHub CLI (gh) がインストールされていません。

【対処方法】
brew install gh
gh auth login

Status: error
```
