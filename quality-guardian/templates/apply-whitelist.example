# .apply-whitelist - パッチ適用ホワイトリスト
#
# このファイルは apply.sh スクリプトで使用されます。
# 変更を許可するパスパターンを記述してください。
#
# 書式:
#   - 1行に1パターン
#   - # で始まる行はコメント
#   - ! で始まる行は除外パターン
#   - ** は任意のディレクトリにマッチ
#   - * は任意のファイル名にマッチ
#
# 使用方法:
#   1. このファイルをプロジェクトルートに .apply-whitelist としてコピー
#   2. プロジェクトに応じてパターンを編集
#   3. apply.sh を実行

# ==============================================================================
# ソースコード（許可）
# ==============================================================================

# TypeScript / JavaScript
src/**/*.ts
src/**/*.tsx
src/**/*.js
src/**/*.jsx

# テストファイル
tests/**/*.ts
tests/**/*.tsx
tests/**/*.test.ts
tests/**/*.spec.ts
__tests__/**/*.ts
__tests__/**/*.tsx

# スタイル
src/**/*.css
src/**/*.scss
src/**/*.less

# ==============================================================================
# 設定ファイル（許可）
# ==============================================================================

# パッケージ設定
package.json
tsconfig.json
tsconfig.*.json

# ビルド設定
vite.config.ts
webpack.config.js
next.config.js
next.config.mjs

# Lint/Format設定
.eslintrc*
.prettierrc*
biome.json

# ==============================================================================
# 除外パターン（絶対に変更禁止）
# ==============================================================================

# 認証・シークレット
!src/config/secrets.ts
!src/config/credentials.ts
!**/*.secret.*
!**/*.key
!**/*.pem

# 環境変数
!.env
!.env.*
!**/.env
!**/.env.*

# Git設定
!.git/**
!.gitignore
!.gitattributes

# CI/CD設定（慎重に変更すべき）
!.github/**
!.gitlab-ci.yml
!Jenkinsfile

# ロックファイル（npmが管理）
!package-lock.json
!yarn.lock
!pnpm-lock.yaml

# ビルド成果物（生成されるもの）
!dist/**
!build/**
!.next/**
!node_modules/**
